import React from "react";
import PaginationWrapper from "../features/pagination";

const page = () => {
    return (
        <div>
            <PaginationWrapper />
        </div>
    );
};

export default page;
